from setuptools import setup, find_packages

setup(
   name='trainers',
   version='0.1',
   description='ML trainers package.',
   author='Howard Liao',
   packages=['trainers'],
   install_requires=['torch'],
)
